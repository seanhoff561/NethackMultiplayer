{
  "name": "nethack-descent",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "First-person real-time window port for the original NetHack engine",
  "scripts": {
    "start": "node server.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node scripts/spatial-browser-smoke.mjs && node scripts/polish-browser-smoke.mjs && node scripts/dark-fantasy-browser-smoke.mjs"
  },
  "dependencies": {
    "three": "0.180.0",
    "ws": "8.18.3"
  },
  "devDependencies": {
    "esbuild": "0.25.10",
    "playwright": "1.55.1"
  }
}
